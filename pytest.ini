[pytest]
log_cli = 1
log_cli_level = INFO
addopts =
    --alluredir=allure-results
    --allure-no-capture
    --tb=short
    -m "not examples"
markers =
    examples: educational/demo tests excluded from default CI runs
